
pages:
  stage: deploy
  script:
    - mkdir .public
    - cp -r ./* .public
    - mv .public/repo/* .public
    - rm -rf .public/repo
    - rm -rf public
    - mv .public public
  artifacts:
    paths:
      - public
  only:
  - main
